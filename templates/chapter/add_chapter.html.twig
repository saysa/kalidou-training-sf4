{% extends 'base.html.twig' %}

{% block body %}

    <h1>Add Chapter</h1>

    {{ form_start(form_add_chapter) }}

        {{ form_row(form_add_chapter.title) }}
        {{ form_row(form_add_chapter.content) }}

        <button type="submit">Add chapter</button>

    {{ form_end(form_add_chapter) }}

{%  endblock %}